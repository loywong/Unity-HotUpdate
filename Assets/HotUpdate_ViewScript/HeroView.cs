using UnityEngine;

public class HeroView : MonoBehaviour {
    // Start is called before the first frame update
    void Start () {
        Debug.Log ("Hero, ViewScript 111");
    }

    public void Init () {
        Debug.Log ("Init");
    }
}